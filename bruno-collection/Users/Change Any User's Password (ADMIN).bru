meta {
  name: Change Any User's Password (ADMIN)
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/users/4/reset-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "teste123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
